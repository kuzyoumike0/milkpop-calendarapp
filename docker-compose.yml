version: "3"
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
