version: "3.9"
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - SESSION_SECRET=please_change_me
      # 下記3つを設定すると Google OAuth が有効化されます（省略可・未設定ならデモログイン）
      # - GOOGLE_CLIENT_ID=
      # - GOOGLE_CLIENT_SECRET=
      # - GOOGLE_CALLBACK_URL=http://localhost:8080/auth/google/callback
