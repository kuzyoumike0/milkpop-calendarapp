import React from "react";

export default function ShareLinkPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-[#FDB9C8]">共有リンクページ</h2>
      <p>ここで生成された共有リンクの一覧を表示します（将来的に拡張可能）。</p>
    </div>
  );
}
