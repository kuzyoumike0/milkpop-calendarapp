{
  "name": "calendar-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/index.js",
    "build": "cd frontend && npm install && npm run build && cp -r build ../backend/frontend_build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "pg": "^8.11.3",
    "uuid": "^9.0.0",
    "cors": "^2.8.5"
  }
}
